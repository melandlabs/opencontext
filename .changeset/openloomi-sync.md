---
---

Synced the incremental OpenLoomi agent-provider changes (Hermes/ACP env + `formatModelId`, Codex `--disable goals`, CLI process tracking) and centralized OpenContext directory paths into `@melandlabs/env-config`. These changes were already versioned and published (`0.3.1` / `0.4.0`), so this changeset intentionally bumps no packages.
